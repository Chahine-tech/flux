/**
 * flux control plane — HTTP server + real-time events (level N3).
 *
 * unstable/httpapi (declarative API + OpenAPI), unstable/rpc + socket
 * (event streaming), internal PubSub, Layer.launch for the lifecycle.
 * Placeholder until N0–N2 are done.
 */
console.log("flux control plane — N0 scaffold, server coming at N3")
